spring:
  application:
    name: retroGamesDTO
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/DN_NAME?useTimezone=true&serverTimezone=UTC
    username: USER_NAME
    password: PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
logging:
  level:
    root: warn
